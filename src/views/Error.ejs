<% layout('layout') %>

<div class="container">
  <h1 class="text-danger">Error</h1>
  <% if (errors.length>0) {%>
  <div class="w-100 alert alert-danger" role="alert">
    <% errors.forEach(error => { %>
    <li><%= error.msg %></li>
    <% }); %>
  </div>
  <% } %>
</div>
